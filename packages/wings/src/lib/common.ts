import type { CallOptions } from "nice-grpc-common";

/** Client call options. */
export type ClientCallOptions = CallOptions;

export { ClientError, Metadata, ServerError, Status } from "nice-grpc";
